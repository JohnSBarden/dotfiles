#!/usr/bin/env bash
ROFI_SELECTION=$(find $HOME/workspace -name ".idea" | sed 's/\.idea//g' | sed "s#$HOME/##g" | rofi -dmenu -p "storm-modi > ")

[[ ${ROFI_SELECTION} != "" ]] && eval '$HOME/.bin/phpstorm $HOME/${ROFI_SELECTION}'
